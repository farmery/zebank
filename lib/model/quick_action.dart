import 'package:flutter/material.dart';

class QuickAction {
  String title;
  IconData icon;
  Color buttonColor;
  QuickAction(
      {required this.title, required this.icon, required this.buttonColor});
}
